# Server-side keys — set these in Vercel (Project Settings > Environment Variables)
ANTHROPIC_API_KEY=sk-ant-...
GROQ_API_KEY=gsk_...

# Flywheel storage (optional — app works without it, feedback then stays local-only)
# Supabase project: Settings > API. Use the service_role key, NEVER the anon key here.
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_KEY=eyJ...
